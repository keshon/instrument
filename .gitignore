# ОС и редакторы
Thumbs.db
.DS_Store
*.swp

# Локальные настройки инструментов (launch.json остаётся в репозитории)
.claude/settings.local.json

# Сгенерированный сайт. Источник — docs/ и docsgen/, здесь только вывод.
site/
docsgen/docsgen
docsgen/docsgen.exe
