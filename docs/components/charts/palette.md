---
title: Категориальная палитра
group: Графики
status: stable
source: src/tokens.css
api:
  - { name: "--chart-1", kind: "токен", doc: "Тон ряда №1. Порядок — часть контракта: ряд №1 всегда этот тон" }
  - { name: "--chart-2", kind: "токен", doc: "Тон ряда №2. Порядок — часть контракта: ряд №2 всегда этот тон" }
  - { name: "--chart-3", kind: "токен", doc: "Тон ряда №3. Порядок — часть контракта: ряд №3 всегда этот тон" }
  - { name: "--chart-4", kind: "токен", doc: "Тон ряда №4. Порядок — часть контракта: ряд №4 всегда этот тон" }
  - { name: "--chart-5", kind: "токен", doc: "Тон ряда №5. Порядок — часть контракта: ряд №5 всегда этот тон" }
  - { name: "--chart-6", kind: "токен", doc: "Тон ряда №6. Порядок — часть контракта: ряд №6 всегда этот тон" }
  - { name: "--series", kind: "переменная", value: "--chart-1", doc: "Приёмник тона. Компонент ряда читает её, а не тон напрямую" }
---

Шесть тонов, различающих **ряды графика**. Единственное место в ките, где цвет
не кодирует состояние, — и потому единственное, у которого свои правила.

```html preview
<ul class="inst-legend">
  <li class="inst-legend-item"><span class="inst-legend-swatch" style="--series:var(--chart-1)"></span>Ряд 1</li>
  <li class="inst-legend-item"><span class="inst-legend-swatch" style="--series:var(--chart-2)"></span>Ряд 2</li>
  <li class="inst-legend-item"><span class="inst-legend-swatch" style="--series:var(--chart-3)"></span>Ряд 3</li>
  <li class="inst-legend-item"><span class="inst-legend-swatch" style="--series:var(--chart-4)"></span>Ряд 4</li>
  <li class="inst-legend-item"><span class="inst-legend-swatch" style="--series:var(--chart-5)"></span>Ряд 5</li>
  <li class="inst-legend-item"><span class="inst-legend-swatch" style="--series:var(--chart-6)"></span>Ряд 6</li>
</ul>
```

## Оговорённое исключение

Первый закон кита требует, чтобы цвет кодировал состояние, категорию или
полярность. Ряд графика — это **категория**, поэтому палитра закон не
нарушает, а уточняет его. Исключение оговорено в
[конституции](../../about/design-principles.md) поимённо, и вместе с ним оговорены пять
правил, которых нет больше нигде в ките.

## Когда использовать

| Используйте | Возьмите другое |
|---|---|
| Различить ряды на одном графике — вместе с [легендой](./legend.md) | **Состояние объекта** — `data-tone` и [бейдж](../display/badge.md): статусные тона закреплены и в палитре не участвуют |
| Метку ряда в легенде того же графика | **Заливку кнопки или подсветку строки** — [кнопка](../actions/button.md) и акцент: категориальный цвет вне графика запрещён |
| Пять-шесть рядов максимум | **Седьмой ряд** — это не график, а [таблица](../display/table.md) |
| Ряд, чей номер известен из данных | **Величину одного показателя** — [мера](./meter.md) или [кольцо](./ring.md): у них один тон, потому что сравнивается одна величина |

## Правила

Пять, и каждое ловит ошибку, которую иначе никто не заметит.

| Правило | Что ловит |
|---|---|
| **Порядок — часть контракта.** Ряд №1 всегда `--chart-1` | Один и тот же показатель, меняющий цвет от экрана к экрану. После этого цвет не значит ничего, и легенду приходится перечитывать на каждой странице |
| **Тона отстоят минимум на 25° от статусных (25, 85, 150) и от акцента (250)** | Категориальный ряд, случайно прочитанный как «упало» или как «нажми сюда» |
| **Светлота внутри палитры разная намеренно** | Ряды, сливающиеся при дальтонизме и на чёрно-белой печати, где тон исчезает вовсе |
| **Категориальный цвет не появляется вне графика и его легенды** | Второй акцент, заведённый чёрным ходом: в роли статуса, заливки кнопки или подсветки строки он запрещён |
| **Шесть — потолок** | Седьмой ряд. Он означает, что показывать надо таблицей, а не графиком: различить семь тонов человек уже не может |

**Легенда обязательна при двух и более рядах.** Правило принадлежит палитре, а
не легенде: без расшифровки цвет ничего не сообщает, и тогда его незачем было
назначать. Подробности — на странице [легенды](./legend.md).

## Тона

| Токен | Тон | Светлота (светлая тема) | Светлота (тёмная) |
|---|---|---|---|
| `--chart-1` | 280 | 0.520 | 0.700 |
| `--chart-2` | 320 | 0.600 | 0.760 |
| `--chart-3` | 355 | 0.550 | 0.720 |
| `--chart-4` | 55 | 0.650 | 0.800 |
| `--chart-5` | 115 | 0.580 | 0.740 |
| `--chart-6` | 190 | 0.620 | 0.780 |

Значения записаны в OKLCH через `light-dark()`: тон и насыщенность держатся, а
светлота переворачивается по теме. Ни один тон не попадает в зарезервированные
коридоры — `25` (ошибка), `85` (предупреждение), `150` (успех), `250` (акцент
и «идёт»).

Ряд №3 стоит на `355`, то есть в 30° от ошибочного `25`: это ближайшее
сближение во всей палитре, и дальше её расширять нельзя не только из-за
различимости, но и потому, что свободных коридоров не осталось.

## Как назначается ряд

Через `--series`, инлайном, **как канал данных**: номер ряда приходит вместе с
данными и не может быть известен статическому CSS.

```html preview
<svg class="inst-sparkline" viewBox="0 0 120 32" preserveAspectRatio="none" aria-hidden="true" style="--series:var(--chart-3)">
  <path class="inst-sparkline-area" d="M0,20 24,14 48,18 72,9 96,12 120,5 120,32 0,32Z"/>
  <path class="inst-sparkline-line" d="M0,20 24,14 48,18 72,9 96,12 120,5"/>
</svg>
```

Переменная ставится **один раз на корень ряда** — на `<svg>` спарклайна, на
метку легенды, — и все узлы ряда наследуют её. Компоненты, читающие `--series`:
`inst-legend-swatch`, `inst-sparkline-line`, `inst-sparkline-area`,
`inst-sparkline-last`. У всех умолчание — `--chart-1`.

Если рядов один, `--series` не ставится вовсе.

```css
/* Именованный ряд приложения — привязка номера к показателю в одном месте */
.chart-render  { --series: var(--chart-1); }
.chart-physics { --series: var(--chart-2); }
```

Такая привязка — самый дешёвый способ выполнить первое правило: показатель
получает номер один раз, а не в каждом шаблоне.

## Чего палитра не делает

| Не делает | Почему |
|---|---|
| Не кодирует полярность | Рост и падение — это [метрика](../display/metric.md) и её дельта, где цвет означает «лучше» и «хуже» |
| Не кодирует состояние | Статусные тона закреплены за `data-tone`, и коридоры разведены нарочно |
| Не даёт последовательной шкалы | Шесть различимых тонов — это категории. Шкала «мало → много» одним тоном разной светлоты в ките не заведена |
| Не растягивается | Седьмого токена нет, и добавить его локально — значит нарушить потолок, а не расширить палитру |

## Справочник

```api
```

### Где читается

| Компонент | Узлы |
|---|---|
| [Легенда](./legend.md) | `inst-legend-swatch` |
| [Спарклайн](./sparkline.md) | `inst-sparkline-line` · `inst-sparkline-area` · `inst-sparkline-last` |
| [Аватар](../display/avatar.md) | Тон группы: аватар различает **сущности**, а не сообщает состояние — та же работа, что у ряда |

[Кольцо](./ring.md) и [мера](./meter.md) `--series` **не читают**: они
показывают одну величину и берут `--tone-mark`.

## Доступность

| | |
|---|---|
| Цвет не единственный носитель | Ряд всегда назван словом в легенде. Палитра различает ряды, но не сообщает о них ничего — сообщает подпись |
| Дальтонизм | Разная светлота внутри палитры выбрана ровно для этого: при потере тона ряды остаются различимы по яркости |
| Чёрно-белая печать | Тот же механизм. Шесть тонов одной светлоты слились бы в один серый |
| Контраст | Каждый тон держит 3:1 против поверхности в шести темах: линия и метка ряда — несущие знаки |
| Подписи | Категориальный тон **не используется для текста**: 3:1 хватает знаку, но не подписи. Текст берёт `--text-primary` или `--tone-ink` |
| Режим принудительных цветов | Категориальные тона сбрасываются системой, и это правильно: различать ряды в таком режиме обязана легенда со словами |

## Связанное

[Легенда](./legend.md) · [Спарклайн](./sparkline.md) · [Кольцо](./ring.md) ·
[Мера](./meter.md) · [Цвет](../../foundations/colors.md) ·
[Бейдж](../display/badge.md) · [Метрика](../display/metric.md)
